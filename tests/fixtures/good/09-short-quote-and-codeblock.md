# Short quotes and code blocks are ignored

The actor used a "Storm-0501" variant of the loader, which is not a four-word direct quote and should not trigger.

Code blocks are excluded from the scanner:

```
The group will pivot to financial services next month.
```

The above is illustrative content in a fenced block and should not lint.
